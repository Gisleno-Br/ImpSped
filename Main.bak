#include <hmg.ch>
#include <minigui.ch>

Function Main

PUBLIC aBackColor := { 57, 108, 164 }

   PUBLIC cBackColor := { 166, 210, 210 }

   PUBLIC bColor := {|| iif( ( LastRec() > 0 ) .AND. ( RecNo() / 2 == Int(RecNo() / 2 ) ), { 222,222,222 }, { 255,255,255 } ) }

   PUBLIC fColor := {|| iif( ( LastRec() > 0 ) .AND. ( AllTrim(Status ) == 'Ativo' ), { 81,81,255 },  { 255, 0, 0 } )  }

   PUBLIC bColorGrid := {|| iif( ( LastRec() > 0 ) .AND. ( RecNo() / 2 == Int(RecNo() / 2 ) ), { 222,222,222 }, { 255,255,255 } ) }
   
   Private nDEsk1 := 975

   PUBLIC cRazaoEmp     := ''
   PUBLIC cFantadisEmp  := ''
   PUBLIC cEnderecoEmp  := ''
   
   
   PUBLIC cBairroEmp    := ''
   PUBLIC cCidadeEmp    := ''

   PUBLIC qTotReg       := 50
   
   PUBLIC nVirtual1     := 540

   PUBLIC nIdEmp        := -1

   PUBLIC cSqlLog       := "c:\temp\Logs\Sqlx1_Log_" + DToS( Date() ) + ".Log"

   PUBLIC cUfEmp        := ''
   PUBLIC cResponEmp    := ''
   PUBLIC cCnpjEmp      := ''

   PUBLIC cTelEmp        := ''
   PUBLIC cDtExp         := ''
   PUBLIC cCepEmp        := ''

   PUBLIC aEmpresa       := {}

   PUBLIC dDataBase      := Date()


			
        Load Window Main
        
      
        
        Main.Center
        Main.Activate

Return
