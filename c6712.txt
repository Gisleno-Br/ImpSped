 select relacao,camposistema,tipo,campo,mascara,tamdisp,consparametros.* from consparametros,dicionario where idconsul = 4 and iddicionario = idcampo and tabela = 'Clientes' order by itemOrdem select relacao,camposistema,tipo,campo,mascara,tamdisp,consparametros.* from consparametros,dicionario,consultas where idconsul = 4 and consultas.id = dicionario.idconsulta and iddicionario = idcampo and consultas.tabela = 'Clientes' order by itemOrdem select relacao,camposistema,tipo,campo,mascara,tamdisp,consparametros.* from consparametros,dicionario,consultas where idconsul = 4 and consultas.id = dicionario.idconsulta and iddicionario = idcampo and consultas.tabela = 'Clientes' order by itemOrdem